// създаваме
let a = 5;
let b = 3;

let c = (a + b);

// извикваме функцията и подаваме параметрите a и b
let n = add(a, b);

// печатаме
console.log(c);
console.log(n);

// създаваме функция
function add(x, y) {
    return x + y;
}
